"""Settings package — imports from the module specified by DJANGO_SETTINGS_MODULE.

When DJANGO_SETTINGS_MODULE is not explicitly set (e.g. local dev without .env),
we default to development settings.
"""
